namespace UVSim
{
    public enum ProgramType
    {
        None = 1,
        FourDigit = 100, // Value is set to the number needed to separate the instruction from the data.
        SixDigit = 1000,
    }
}
